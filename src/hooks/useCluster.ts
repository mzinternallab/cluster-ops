// Cluster switching and kubeconfig parsing
// Implemented in Phase 1 Step 4-5

export function useCluster() {
  // TODO: invoke('get_kubeconfig_contexts') and invoke('set_active_context')
  return {}
}
